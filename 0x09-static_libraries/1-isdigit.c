#include "main.h"

/**
 * _isdigit - checks if the entry is a digit between 0 and 9.
 * Return: returns 1 if true and 0 otherwise.
 * @c: the entry to be checked.
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
